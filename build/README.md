# Build Output

This directory is the local destination for generated Windows and Linux debug exports. Run `make export-windows` or `make export-linux`; the tooling creates platform subdirectories as needed.

Generated binaries and data packs are intentionally ignored. Do not place source assets or hand-authored content here.
