# Documentation Map

Product intent is under `product/`, feature rules under `design/`, technical contracts under `engineering/`, accepted decisions under `decisions/`, active/completed work under `exec-plans/`, and manual evidence notes under `playtests/`.
