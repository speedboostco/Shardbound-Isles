# Build and Release

Stable commands are exposed through `Makefile` and `scripts/dev.ps1`. `GODOT_BIN` may identify a Godot executable; otherwise the script searches PATH and common local locations. Debug exports target Windows and Linux under `build/`. Publishing always requires human approval.
