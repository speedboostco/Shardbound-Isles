# Save System

Persistence is not part of the first playable. Its contract is versioned JSON-compatible state, stable definition IDs, atomic replacement where practical, deterministic island and item restoration, and explicit rejection or migration of unsupported schemas.
