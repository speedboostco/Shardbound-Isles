# Testing Strategy

## Tasks 31-45 visual and Legendary gate

Unit coverage validates deterministic facing, semantic atlas/import contracts including binary alpha and the 16-color cell ceiling, reduced-effects bounds, every Legendary event hook, idempotent duplicate-free lifecycle, authored parameter consumption, Chain Mining radius/uniqueness, Burning Smelter one-shot eligibility, and Living Arrows cap. Integration instantiates both visual and gameplay scenes, preserves collision/damage contracts, proves the Slime tell, Ranger silhouette, VFX cleanup, five distinct rarity shapes, shared focus style, visible/consumable Smelter value, confirmed-impact Living Arrows behavior, installed-island kill propagation, all three equip/use/unequip routes, and summon cleanup.

The fixed seed `314159` gate simulation covers gather/combat/first item, controller-opened comparison, two-press salvage, all three Legendary effects, 200 repeated lifecycle syncs, five-plus enemies, 200 drop pressure, and 300 VFX cleanup. It prints `VISUAL_LEGENDARY_GATE_METRICS`. Eight reproducible 1280x800 captures cover the art scene, forest loop, dense combat/loot, three Legendary identities, comparison, and destructive confirmation.

The project uses a small dependency-free Godot test harness under `game/tests/` to avoid an unreviewed dependency. Unit tests cover pure rules; integration tests instantiate cooperating nodes; simulation tests drive fixed actions and assert player-facing milestones. All procedural assertions use fixed seeds. Visual scripts produce reproducible 1280x800 artifacts for manual inspection.

Every CLI test command writes a JUnit report under `build/test-results/`; set `TEST_REPORT_DIR` to choose another directory. A missing script, compile failure, assertion failure, incomplete assertion contract, or report-write failure returns a nonzero exit code. `validate` imports the project, runs static validation, and then runs the complete test suite.

CI runs import, static validation, unit, integration, and simulation steps separately so logs identify the failing layer. JUnit reports and successful platform exports are retained as workflow artifacts.

To add a test, create a deterministic script in the matching `game/tests/<layer>/` directory, implement `run(support)` for unit tests or `run(support, scene_tree)` for asynchronous scene tests, and register its path and exact assertion count in `game/tests/run_tests.gd`. Run the narrow layer first, then `make validate`.

The M1 simulation repeats the same fixed-seed route from spawn to tree, stone, first Slime, weapon pickup/equip, and second Slime. It asserts exact repeatability, inventory totals, two deaths, one optional item drop, canonical item state, and fewer equipped hits. The M1 scene contract separately measures coalescing 100 resource drops and validates actual input actions, camera behavior, interaction, attack cooldown, and Slime state.

M2 unit coverage validates all registries, rarity contracts, affix eligibility/conflicts, JSON round trips, six-slot stat recomputation, protected salvage, and modular legendary lifecycle. A fixed-seed 10,000-item run checks every generated item for duplicates, conflicts, eligibility, and serialization. Integration tests exercise the actual sword, bow, and wand target shapes, controller equipment flow, schema-4 migration, each new legendary world effect, temporary cleanup, and a 200-drop cap scenario. The M2 simulation generates a mixed six-slot loadout and triggers Chain Mining, Burning Smelter, and Living Arrows with exact metrics.

The Tasks 16-30 conformance layer adds explicit definition diagnostics, drop/no-drop RNG boundaries, `instance_id` compatibility, required/excluded tags, level/tier eligibility, ordered rejection reasons, exact roll ranges/counts, mutation events, duplicate ownership rejection, projectile lifecycle, rejected-pickup preservation, pure tooltip/comparison mapping, and two-phase salvage confirmation. Its fixed simulation performs 500 seeded equipment operations, clears back to exact base stats, then salvages 48 items while checking material conservation. Seven reproducible 1280x800 captures cover M1 normal/damaged/pickup/equipped states plus short, long, empty-slot, comparison, and salvage-warning UI states.

M3 unit coverage validates the Node-free archipelago, deterministic neighbors, shard/runtime separation, required definition fields, modifier component lifecycle, all three adjacency tradeoffs, schema-5 persistence, and schema-4 migration. A 10,000-shard fixed-seed generation checks all six biomes and paired risk/reward validity. Integration exercises controller preview/slot focus, transactional failure, physical Forest content, all cleanup/protection rules, Moonleaf crafting, adjacency preview, destroyed resource and reward restoration, and replacement warnings. The fixed M3 simulation performs the complete obtain-preview-place-synergize-save-replace-remove route.

M4 unit coverage validates registry IDs and atomic recipe plans; preview-free placement; FPS-independent mill conversion; collector rarity/ownership/radius/batch limits; safe overflow; clock rollback/four-hour cap; +0…+10 cost/power/preservation; and schema-5→6 migration. Integration performs controller placement and rotation, physical multi-building flow, rare/player-owned exclusions, blocked output, protected upgrade, and save/load continuation. The fixed M4 simulation repeats the full manual-resource→automation→upgrade route and prints `M4_METRICS`.

The exploration-progression suite unit-tests mana bounds/regeneration, technology prerequisite/cost/restore invariants, twelve graph nodes with icon/recipe metadata, all nineteen recipe definitions, and contact-shadow profiles constrained to the sprite baseline. Integration verifies the title/stat HUD removal, inventory ownership of numbers, opening/focus/return behavior of the visual technology page, its twelve rendered nodes and recipe descriptions, controller learning, dormant-to-awakened enemies, advanced combat/gathering/mana/production crafted-effect consumers, shared live/offline production scaling, dressed terrain/resident density, empty-space bow feedback, magic cost, and serialized technology/mana state. The fixed first-playable simulation now gathers the opening cost, learns both required technologies, and only then enters combat.

The survival/journey unit suite validates non-draining preparation, three-ration capacity, bounded rest recovery, six prepared yields, eleven ordered milestones, one-time rewards, atomic ration crafting, and primitive-data restoration. Expedition unit coverage fixes every phase boundary, clock wrap, seed/weather repeatability and separation, night tradeoff, Shelter expiry, forage regrowth, and invalid restore. Integration exercises four real forage nodes, weather/night pickup resolution, live enemy speed, player protection/mana regeneration, exact regrowth boundaries, HUD lighting/status, schema-10 round-trip, and schema-8 migration without reward duplication. Reproducible 1280x800 captures cover the camp/forage field, night Shelter status, and Journey page.

The Wayfinder contract suite validates all four weather definitions, deterministic reward seeds, state transitions, exact target clamping, stable event-token deduplication, one-shot claim, and primitive restore rejection. Scene integration covers Mira offer/decline/re-offer, controller focus, acceptance, three physical fog events, ordinary event loot, duplicate callback defense, completion/turn-in, rare equipment and shard pickup, inventory marks, claimed-save replay, schema-9 migration, new-day rollover, and bounded visual density for every weather. Two 1280x800 artifacts capture the offer decision and the active fog-event route.

The installed-island story suite validates deterministic definitions across all six biomes, stable island binding, every state transition, restoration/purge reward separation, duplicate-event rejection, one-shot claim, three one-time shop offers, serialization, and invalid restore. Scene integration covers installation, Tala, controller focus, two survey sites, explicit branch choice, two branch sites, the biome Warden, visible reward pickup, Mark spending, schema-11 round-trip, schema-10 migration, and complete scene cleanup after removal. Separate 1280x800 artifacts cover the renewed starting terrain and the branch decision.

The Forest Warden suite validates phase thresholds, seed-repeatable attack ordering, separated seeds, 0.75-second-or-longer root warning, faster second-phase cadence, bounded symmetric thorn fans, finite root geometry, danger-lane hits, safe side space, and long-index safety. Scene integration verifies the unique atlas, twelve-health/no-contact profile, five non-empty offline audio cues, Tala's event-driven guidance, exact telegraph/projectile correspondence, phase transition, branch-aware defeat line, projectile/audio cleanup, suppression of ordinary island threats, and the absence of installation/event/pedestal circular zones. Two 1280x800 captures cover thorn windup and the phase-two root lane.
